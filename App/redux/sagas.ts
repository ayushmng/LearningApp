import {all, fork} from 'redux-saga/effects';
// import {watchGetCategoryList} from './GetCategoryList/GetCategorySaga';

export default function* rootSagas() {
  // yield all([fork(watchOrderListSaga)]);
}
